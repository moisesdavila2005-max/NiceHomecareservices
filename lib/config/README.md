# Configuration Files

This directory contains all application configuration:

- `routers.ts` - TRPC router definitions
- `resend-config.ts` - Email service configuration and tests

All configuration files from the root have been consolidated here for better organization.
