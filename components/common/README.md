# Common/Utility Components

Reusable utility components used throughout the app:

- `logo/` - Logo component
- `visits-counter/` - Visit tracking component

These are simple, reusable components used in multiple places.
